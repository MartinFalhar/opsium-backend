# Příklad konfiguračního souboru pro backend
# Vytvořte kopii tohoto souboru jako .env a vyplňte hodnoty

# JWT Secret - použijte silný náhodný řetězec (min. 32 znaků)
# Můžete vygenerovat například pomocí: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your-secret-key-change-in-production

# GoSMS credentials (pokud používáte)
GOSMS_CLIENT_ID=your-gosms-client-id
GOSMS_CLIENT_SECRET=your-gosms-client-secret

# Database configuration
DB_HOST=localhost
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=your-db-name
DB_PORT=5432

# Port pro backend server
PORT=3000
